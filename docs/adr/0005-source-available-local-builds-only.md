# Source available, local builds only

The source is public, but the first version is built and installed locally by each user. App Store distribution, notarization, a public installer, automatic updates, and broad macOS compatibility are deliberately out of scope. Adding any of them requires a separate decision so the project can remain small and lightweight.

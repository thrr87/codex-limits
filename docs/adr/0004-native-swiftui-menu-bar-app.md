# Native, lightweight SwiftUI app for macOS

The app targets macOS only and uses Swift, SwiftUI, `MenuBarExtra`, Apple Swift Charts, and `ServiceManagement`. It has no Electron or Tauri shell, third-party chart library, web server, or additional runtime. This keeps memory overhead low and behaviour native at the cost of portability to other platforms.
